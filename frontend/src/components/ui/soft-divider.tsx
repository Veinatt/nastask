/** Vertical translucent separator for interval card meta rows. */
export function SoftDivider() {
  return (
    <span
      className="mx-1.5 h-3.5 w-px shrink-0 bg-[hsl(var(--foreground)/0.2)]"
      aria-hidden
    />
  )
}
